# 3D 预览重构说明（本地版，未推送）

改动集中在 `preview3d.js`，`index.html` 版本号 `v31 → v39`。

---

## 零之零零零零零零、v39：悬空刀口 bug（N001 那两条围边的真相）

用户问：「N001 就差那两条围边看着别扭，是不是折线反了？」

**用户的直觉是对的——那两条围边确实不该是独立片。但原因不是折线反了。**

### 四步追查，每一步都用数据证伪

| 步骤 | 假设 | 证据 | 结论 |
|---|---|---|---|
| 1 | 折线方向反了 | fe[0] 是**压痕**（非切割），横在 y=50、x=122..322，正是 P0 上边缘 | ✗ 方向没错 |
| 2 | 中间真是废料 | y=30 的切割线覆盖 x=0..122 和 322..443，**唯独中间 x=122..321 没有** | 存疑 |
| 3 | 竖直方向被切断 | 沿 x=220 竖扫：**从 y=0 到 y=400 只有 2 个交点**（上下边界），中间没有 | **纸面是连续的** |
| 4 | 那是谁划出来的？ | 查 y=50 处端点邻接：**只有 fe[4]、fe[20]，无任何线与之相连** | **下端悬空** |

### 根因

```
fe[4]   (122,50) → (122,30)   ← 竖切线，下端悬空
fe[20]  (322,30) → (322,50)   ← 竖切线，下端悬空
```

这两条是**让位刀口（单线 slit）**——给端墙让位，让它能折起来。它们是刀模里的真实结构，但**单线刀口不闭合，不应该翻转内外判定**。

奇偶算法却把每条切割线都算成交点，结果在纸面中间凭空划出一条 21mm 的「废料带」，把 50mm 高的端墙（29mm 残片 + 21mm 带）**拦腰截断**成一块残片 + 一条漂浮的独立片。这正是用户看到的别扭之处。

### 修复：剪除悬空刀口（`_pruneSlitCuts`）

反复移除「端点度为 1」的切线段。闭合环的顶点度都是 2，所以真正的轮廓和正规双线槽口都能保留。

**三个条件必须同时满足，少一个就会误伤**（全是回归逼出来的）：

1. **长度上限** ≤ 毛坯对角线的 5%（刀口是短的）
2. **悬空端必须在纸面内部**（距毛坯 bbox > 对角线 3%）—— 断口轮廓在边缘，刀口在中间
3. **总量熔断**：若剪掉超过 50% 的切割总长则整体放弃，防止轮廓本身有导出缺口时被吃光

调试过程：

| 写法 | 后果 |
|---|---|
| 只按悬空，不限长度 | A009 1%→**80%**、B009 15%→**97%** |
| 限长 6% / 8% | 0308 6%→**54%** |
| **限长 5% + 内部性判据** | **全部回退消失** |

### 结果

- **N001：岛数 3 → 1**，端墙从 199×29 的残片变成 **199×48 的完整端墙**（正是 L×D = 200×50）✓
- **Q013：26% → 20%**，Z 轴 301 vs 设计 300
- 原样本：good 15/22、中位 6%（与基线持平）
- **错开样本：12/25、中位 15%——与修改前完全一致，零回归**

### 这轮最该记住的一条

**用户说"不对"时，先怀疑"我以为对的地方"。**

我上一轮用「沿列采样 grid 全为 0」就下了「这是真空间隙」的结论。但 **grid 本身就是奇偶判定的产物**——拿待验证系统的输出去证明该系统正确，是循环论证。

正确做法是回到**原始 fe 数据**独立验证：扫竖线交点、查端点邻接。这次正是靠这两步才翻的案。

---

## 零之零零零零零、v38：把"不折叠"类特例全部删掉，并换样本验证

### N001 的剩余部分——已到结构极限，不再改

查实两件事：

1. **侧墙 P1/P2 内部连切割线都没有**（用"线段与多边形求交"逐个扫过所有 8 条压痕和全部 `fe` 线，命中数为 0）。所以 338mm 是一整块刚性板，那 38mm 超出是真实的卷边结构。
2. **两条围边 P7/P8 与底板之间的 21mm 是真空间隙**（沿列逐像素采样，grid 全为 0）。它们在刀模里本来就独立。

结论：N001 停在 12% 是合理的。

### 静态铰链彻底取消

把 `staticHinge` 的阈值从 0.02 一路降到 -1（永不触发）：

- C023 **72% → 63%**
- JP012 的 X/Y 从 92×87 → **83×82**
- 无任何回退

到这一步，"不折叠"这一类特例已经全部删除：静态铰链、`foldMult=2`、安全阀、`maxFold 0.98`。**每一个都是删掉之后指标变好的。**

### 换样本验证——发现之前高估了

之前六轮都在同一批 22 个盒型上迭代。这次把采样起点错开，取到**完全不重叠**的另外 25 个：

| 样本 | 合格率 | 中位误差 |
|---|---|---|
| 原样本（22 个） | 15/22 = 68% | 6% |
| **新样本（25 个，无重叠）** | **12/25 = 48%** | **15%** |
| 合并 47 个 | **27/47 = 57%** | 约 10% |

**真实水平是 57%，不是 68%。** 只在固定样本上迭代会显著高估效果——这是这轮最值钱的教训。

新样本里做到 1% 误差的：M090、0020、A012、C001GX、E048、J005、JP013、K002、K032、N004、Z008

### 整体进展

| 阶段 | 中位误差 | 合格率 |
|---|---|---|
| 最初（v32） | 141% | 约 5% |
| v34 | 40% | 18% |
| v35 | 23% | 45% |
| v36 | 11% | 55% |
| **v38** | **约 10%** | **57%** |

---

## 零之零零零零、v37：立原则，而不是加特例

### 先否掉了原定方案

原计划做「面板细分」——沿内部压痕把过长的面板切开。诊断后发现 **N001 的侧墙 P1/P2 内部根本没有压痕**，这条路对它不成立。

顺带修正一个误判：N001 的理想值 338×200×52 与设计的 300×200×50 只差 38mm，**那是卷边的真实特征，不是 bug**。真正要修的是"实际渲染 397×251×202 偏离理想值"。

### 真因一：根节点选错了（我上一轮引入的回归）

用「压痕连通度最高」选根，N001 选中的是 **P6——一块 18×338 的窄条**。它夹在两道 299mm 长压痕中间，连通度 598，反而高过底板 P0（199×298）的 298。

结果是铰链链深达 6 层，每层折 90° —— 折到第 5、6 层已经绕成螺旋。

改回**按最大面积选根**：HC104BD 41%→**18%**。（`Preview3D._rootBy='crease'` 保留，默认面积优先。）

### 真因二：角接触产生了零长度铰链（新增原则）

两条 199×29 的围边（P7/P8）与底板 P0 之间隔着 21mm 空隙够不着，只在**角点**上碰到墙板 P1。

于是生成了一条 `len=0` 的退化铰链——轴向量毫无意义，面板就跟着立起来的墙板甩出去 199mm。

**新原则：铰链必须有真实的共享边**（默认 ≥4mm，`Preview3D._minHingeLen` 可调）。

> 关键坑：只在建树处过滤**没用**。Pass2/Pass2b 救援流程是自己构造 overlap 的，绕过了主过滤——必须在那里也加同样门槛。

N001 **51% → 12%**，岛数 1→3（P7/P8 成为独立片，这本来就是对的）。

### 结果

| 指标（22 盒） | 上轮 | 本轮 |
|---|---|---|
| 合格（误差 ≤15%） | 14/22 | **15/22** |
| 中位误差 | 6% | **6%** |

岛数几乎未变（单岛 33/45 保持不变）

### 方法论沉淀

这三轮改下来，有效的动作有个共同点——**都是"少做"而不是"多做"**：

1. **先算"理想值"再动手**：实际 vs 解析式理想的差距表，能立刻分清"渲染端特例"和"结构本身的问题"，省掉大量瞎试。
2. **优先怀疑自己上一轮引入的启发式**：这轮两个真因，一个是上轮回归，一个是缺失的基本原则。
3. **过滤要覆盖所有代码路径**：救援流程绕过主过滤是反复出现的坑。

---

## 零之零零零、v36：安全阀本身也是特例

用户追问「还剩四分之一不对，改进了嘛」。

### 关键手法：把"实际"和"理想"摆在一起

用 `_diag_gap.js` 给每个问题盒型同时算出两个数：实际渲染的折叠尺寸，以及解析式模型（根穷举 + 全部折 90°）能达到的最好尺寸。

| 盒型 | 设计尺寸 | 实际渲染 | 解析式理想 | 判断 |
|---|---|---|---|---|
| N001 | 300×200×50 | 397×251×202 | 338×200×52 | 差很多 → 渲染端有问题 |
| JP012 | 75×75×50 | 92×87×82 | 80×80×30 | 差很多 → 渲染端有问题 |
| HC104BD | 300×200×80 | 329×304×204 | 302×202×132 | 差很多 → 渲染端有问题 |
| E045 | 300×200×150 | 308×298×250 | 306×298×249 | 几乎一致 → 结构问题 |

**差距大就说明渲染端还有残余特例**，不用去动树。这个判断省了大量瞎试。

### 修复

1. **安全阀默认关闭**（`Preview3D._useValve` 可选开启）
   规则是"子面板中心离折线超过自身尺寸 1.2 倍就不折"。看着稳妥，实际**冻结了大量好铰链**——本轮最大收益就来自关掉它。
2. **`maxFold` 0.98 → 1.0**
   原来每个铰链少折 1.8°，让闭合的盒子留条缝。单测不是主因，配合上一条更干净。

### 结果

| 指标（22 盒） | 上轮 | 本轮 |
|---|---|---|
| 合格（误差 ≤15%） | 12/22 | **14/22** |
| 中位误差 | 11% | **6%** |

多数盒型已经压到 **1%**：0017 / A009 / C001F / D038 / J002 / K029 / M041 / G005 / D053A
R012 23%→**5%**、JP021D 11%→**9%**、Q013 29%→**26%**
岛数未回退（33/45 单岛）

### 试了一整轮但没采用：按设计尺寸反选根节点

既然 `de.op` 里有真实的 L/W/D，何不直接**拿它当标准去挑基础面板**？试了四种写法：

| 写法 | N001 | HC104BD | C023 |
|---|---|---|---|
| 无限制 | 51%→**35%** | 41%→**18%** | 72%→**89%** ✗ |
| 覆盖惩罚 0.8 | 51% | 41% | 89% ✗ |
| 带 5% 容差的惩罚 | 51% | 41% | 89% ✗ |
| 硬过滤 reach≥90% | 51% | 41% | 89% ✗ |

前两个盒型能救回来，但 **C023 无论怎么调都变差**。根因是解析评分只覆盖 Dijkstra 树，而实际渲染后面还有 Pass2/Pass2b 救援流程补上面板——**两个口径对不上**。

结论：**净收益为零**，默认关闭（`Preview3D._pickRootByFit`），代码留着。等评分口径和渲染一致了再开。

### 剩下的 8 个（各有各的结构性原因）

| 盒型 | 误差 | 已定位的原因 |
|---|---|---|
| C023 | 72% | 未深挖 |
| N001 | 51% | 侧墙 P1/P2 长 **338mm**，而 W 只有 300——两端各 19mm 拐角延伸没折过去 |
| JP012 | 42% | 未深挖 |
| HC104BD | 41% | 4 个岛 |
| E045 | 33% | 结构本身，解析式理想值也是 33% |
| L015 | 33% | 结构本身，解析式理想值也是 33% |
| Q013 | 26% | 前襟片与应挂靠面板隔 35mm，超过桥接半径 |
| B009 / B048 | 15% | 边缘 |

**下一步方向很明确：面板细分**——把过长的面板沿其内部的拐角压痕切开。N001 的侧墙就是典型：338mm 里有两道拐角压痕，切开后两端各 19mm 就能折过去。

---

## 零之零零、v35：砍掉"自作聪明的特例"

用户反馈「Q013 不对，其他几个还可以」。

### 方法：解析式折叠求值器

写了一个不建网格的求值器（`_diag_root.js` / `_diag_sign.js`）——用矩阵累乘直接算折叠后包围盒。它的价值在于能**瞬间回答"换个根/换个方向会怎样"**，而不用反复渲染截图。

### 两个发现

**① 折叠方向规则没问题**

把「朝父级局部 -Z 折」换成「朝已放置面板的质心折」，两种规则算出来的结果**完全一致**——说明原规则本来就是向内的。省下了一次大改。

**② 渲染器里的特例正在把好结果搞坏**

拿解析式「全部折 90°」对比实际渲染，差距惊人：

| 盒型 | 解析式（全折 90°） | 实际渲染 |
|---|---|---|
| T004A | 200×120×100 | 200×120×100 ✓ |
| D038 | 302×202×101 | 302×202×101 ✓ |
| N001 | 误差 13% | **误差 66%** |
| C023 | 误差 17% | **误差 81%** |

也就是说，基础规则是对的，是后来叠加的**例外处理**在拖后腿。

### 修复（全是删代码）

1. **静态铰链基本取消**（阈值 0.25 → 0.02）
   漏检一次压痕就让整面墙板摊着不动，代价远大于收益。
2. **`foldMult=2` 默认关闭**
   原规则假设竖直链都是墙板序列，链一深就把面板折穿盒子。改为 `Preview3D._useMult2` 可选开启。
3. **建树改用 Dijkstra 最短路树**（代价 = 1/压痕长度）
   最大生成树只管总权重最大，不管离根多远，于是墙板会绕道挂到一块小襟片上。

### 结果

| 指标（22 盒抽样） | 修复前 | 修复后 |
|---|---|---|
| 合格（误差 ≤15%） | 10/22 | **12/22** |
| 中位误差 | 23% | **11%** |

M041 31%→5%、C001F 23%→5%、E045 50%→35%、N001 66%→52%、B048 23%→17%
岛数未回退（33/45 单岛）

### 试过但更差，已回退

**加大桥接距离**（随网尺寸自适应，最多 30mm）：中位 11%→**28%**，Q013 29%→47%，HC006 4%→86%。过大的桥接会连上本不该连的面板，保持固定 +8px。

### Q013 仍未解决（已定位到根）

- 结构特殊：26 块面板、2 个岛、竖向长条（1074×1810）
- 前襟片 **P8（391×78）与它该挂靠的 P7 之间隔着 35mm**，超过桥接搜索半径（24.6mm）
- 于是只能经一个 **26mm 的锁耳 P24** 挂上去 → 绕着这么短的轴旋转 → 整块襟片甩出盒外
- 加大桥接能连上，但全局明显变差 → **两难**，暂时保持现状（误差 29%）

---

## 零之零、v34：找到"散架"的真正原因（第三轮）

### 关键认知修正：换指标

前两轮一直在优化「岛数」，但**岛数少 ≠ 视觉上不散**。第三轮改用客观指标——**折叠后的包围盒 vs 设计尺寸 L/W/D**，误差取「超出量 ÷ 盒子最大边」——才看到真相：

> 22 个盒型抽样，中位误差 **141%**，几乎没有一个真正折成盒子。

T004A 之所以看起来对（误差 9%），只是个例。

### 三个发现

**① 拼版图（一版多盒）**
R012 的 2 个岛尺寸是 [247×212] 和 [259×209]，0308 是 [410×313] 和 [398×299] —— 两个几乎相同的半片。拿整版去比单个盒子的 L/W/D 本身就不对，指标因此改为**只测最大的那个岛**。

**② 测量陷阱（差点毁掉整轮）**
浏览器一直在加载缓存的 v923，导致"改了代码却毫无效果"的假象。每次测量必须加 `?cb=$(date +%s)` 强制重载。

**③ 核心 bug —— 远距离桥接点污染几何**

D038 的两块**主体墙板**（295×99 = L×D）被判定为不折叠，盒子永远立不起来，超出量 374mm。追下去发现：

```
edge2 13-6  len=300  ptsBBox=[125,20 .. 423,21]   ← 接触点在 y≈20
            ov.cy = 25                             ← 但铰链线算在 y=25
            cov = 0                                ← 于是判定"没有压痕"
实际最近压痕距离 = 0.49mm                          ← 压痕明明就在那儿
```

根因：桥接搜索半径 `REACH2 = 9 像素 = 12.6mm`。**跨间隙找到的远距离接触点被混进了质心和 PCA 统计**，把真实的铰链线拽偏了 4.5mm。偏偏接触点数组只存前 600 个（扫描顺序），所以 `pts` 和 `ov` 对不上，压痕覆盖率算出 0。

### 修复

1. **几何只统计近距接触点**（`r2 <= WALL_REACH`），远距离接触只累计计数用于连通性；桥接对保留，几何退化时才回退到远点。
   → D038 接触点命中率 **0/120 → 120/120**
2. **cov 改为"接触点到压痕线的直接邻近度"**（空间网格加速），不再拟合直线判共线。
   → HC006 压痕检出 **25/36 → 32/36**
3. **压痕阈值 0.25** —— 实测数据有清晰断层（要么 ≥0.37 要么 ≤0.09）
4. **根节点改用"压痕连通度最高"的面板**（主板邻接多、襟片只邻接一个），而非最大面积
5. 静态铰链阈值做成可调开关 `Preview3D._staticCov`；实测 0.25 / 0.10 / 0.001 结果一致，证明**它不是主因**（一度被误判）

### 结果

| 指标（22 盒抽样） | 修复前 | 修复后 |
|---|---|---|
| 合格（误差 ≤15%） | 4/22 | **10/22** |
| 中位误差 | 40% | **23%** |
| 岛数 | 33/45 单岛 | 33/45 单岛（未回退）|

| 盒型 | 修复前 | 修复后 |
|---|---|---|
| D038 | 125% | **5%** |
| D053A | 58% | **1%** |
| Q013 | 264% | **29%** |
| K029 | 40% | **6%** |
| 0308 | 55% | **7%** |
| R012 | 75% | **23%** |

---

## 零、v33：解决"盒子散架"（第二轮）

### 诊断

三个决定性发现：

1. **多岛盒型的悬空端点为 0** —— 刀线轮廓是闭合的，缺口不是"轮廓没连上"造成的。
2. **沿压痕垂直探测这条路不可靠**：JP033 的 189 条压痕里，**78% 的采样点向外走 12mm 都落不进任何面板**，结果最大的两块面板（P0/P1，各约 83000mm²）一个铰链都没有。
3. JP033 的网是 2619×952 的横排布局，纸面填充仅 31%。

### 修复

**① 像素级邻接（核心）**

不再靠压痕去"发现"邻接 —— 栅格自己就知道谁挨着谁：从每个区域像素朝 4 个方向跨过线墙走出去，记下对面的第一个区域，累积接触点后用 PCA 求出共享边方向。

压痕的角色降级为**排序**：共享边上有压痕就是真折叠（排前面），纯切割接触排在后面。

**② 两级邻接**

| 层级 | 判据 | 权重 |
|---|---|---|
| tier1 贴墙 | 跨过线墙即相邻 | 正常 |
| tier2 桥接 | 跨过 8px 间隙 | score × 0.08，排最后 |

桥接只在"没有别的方式能连上这块面板"时才会被用到，且**不改变面板形状**。

**③ 森林分根挪到救援之后（顺序 bug）**

原来先给孤立面板分配新根 → `vis` 全部为 true → 后面的 Pass2/Pass2b 救援流程被**整体跳过**。T004A 的 28×18mm 锁片离侧板只有 2mm，本可被几何救援接上，却成了独立岛。

**④ 安全阀**

真压痕必然沿着面板自身边缘，所以子面板中心到折线的距离不可能超过自身尺寸。超过 1.2 倍即判定为误连 —— **保留连接**（这正是避免散架的关键）但 `foldMult=0` 不旋转：一块面板平贴着走，远比凭空甩过来好看。

### 结果

| 指标 | V32 | V33 |
|---|---|---|
| 单岛 | 51% | **73%** |
| 1~2 岛 | 67% | **89%** |
| 最差岛数 | 15 | **7** |
| 使用真实刀模形状 | 80% | **87%** |

铰链质量：758 条中仅 3 条几何异常，均已被安全阀置为不旋转。

> 踩坑：`isForestRoot` 用 `var` 声明在静态铰链检测之后，变量提升成 `undefined`，导致每个盒型都抛 `Cannot read properties of undefined`、铰链数全为 0。声明位置必须提前。

---

## 一、找到的两个真 bug

### 1. 栅格化的内外判定错误（最严重）
把切割线和压痕线**都**当成纸张边界做奇偶射线判定，结果纸面变成棋盘格：相邻面板交替"内/外"。

表现：T004A 的两个主箱体面板（118×198mm，最大的两块）**整块消失**，只剩侧板和襟片。

修法：奇偶判定只用 `style=0` 的切割线（压痕永远在纸张内部，不该翻转内外）；墙体判定仍用全部线。
纸面填充量 `85741 → 131209 px`（+53%）。

### 2. 面板与多边形的映射方式错误
原来用"矩形质心 ↔ 栅格质心距离"配对，结果是：4/16 个面配不到多边形、面积比值出现 0.26 和 1.27（明显错配）。

改法：**栅格面板本身作为唯一真源** —— 它的包围盒喂给铰链树，它的多边形 1:1 用于渲染，不再有映射环节。
T004A：14 面 / 13 铰链 / 零回退，主面板比值 0.96–0.98，异形插舌 0.62–0.91（本就该小于包围盒）。

---

## 二、三项结构性改进

### 压痕驱动邻接
铰链不再靠"包围盒共边"判断。共边判定会被三件事破坏：栅格沿每条线烧出的墙体、轮廓简化的误差、粗分辨率（`res=3.84mm` 时简化容差 7.7mm > 固定容差 3mm，导致 Q011 有 89 个面板却**一个铰链都连不上**）。

改成沿每条压痕线采样，向两侧探测所属面板 —— 压痕在哪，折叠就在哪，与间隙和分辨率无关。

### 碎屑合并
每条线都被烧成墙，所以圆角离散化的弦、差 1px 没闭合的槽口都会削下细条。用**填充率**（区域占自身包围盒的比例）识别：实心面板 ≈1.0，细条 <0.25。
注意邻居搜索必须**跨过墙体**向外走 —— 直接看 4 邻域只能看到墙体（label = -1），一处都合并不了（这个坑排查了一轮）。

Q011：89 → 54 面板；HC013：61 → 51。

### 森林 + 按盒型择优
- **森林**：部分刀模的外轮廓是分段导出的，面板图天然是几个孤岛。原来只长一棵树，孤岛外的面板全部摊平不动；现在每个孤岛各自生根独立折叠。
- **择优**：栅格和矩形两套方案**各有胜负**，没有一方占优（E039A：栅格 89% vs 矩形 4%；JP012：栅格 29% vs 矩形 100%）。所以在建场景前廉价预演一遍，比较**岛数**，少者胜。

> 这里踩过一个坑：岛数统计漏了第一个岛（返回 0 而不是 1），导致矩形方案被系统性偏好，栅格方案的好结果被埋掉。

---

## 三、观感改造（针对"透明塑料薄片"）

| 项目 | 之前 | 现在 |
|---|---|---|
| 板厚 | 零厚度平面 | 挤出实体，0.5–2mm，随网尺寸自适应 |
| 材质 | 白色 Lambert，半透明 0.30/0.72 | 不透明 Standard + 程序化纸板纹理（纤维噪点 + 横向纤维） |
| 切口 | 无 | 独立的瓦楞芯材质（深暖色 + 瓦楞条纹） |
| 光照 | 环境光 0.8 + 两盏平行光 | 半球光 + 带阴影主光 + 冷补光 + 轮廓光 |
| 阴影 | 无 | 阴影贴图 + 地面接触阴影（跟随折叠重新定位） |
| 背景 | 纯灰 `0xf0f0f0` | 竖向渐变影棚背景 |
| 轮廓 | 1px 淡线，单面 | 双面轮廓，颜色加深 |
| 相机 | 按展开图尺寸固定 | 随折叠进度在"展开/折叠"两个取景间插值 |

---

## 四、量化结果（45 个盒型抽样）

| 指标 | 改前 | 改后 |
|---|---|---|
| 单岛（完整一体成型） | 38% | **51%** |
| 单岛 + 2~3 岛 | 65% | **78%** |
| 使用真实刀模形状 | 0% | **80%** |
| 面板配不到多边形 | 4/16（T004A） | **0** |

---

## 五、已知遗留

- 约 11% 仍是 4 个以上孤岛：HC006A(7) / JP033(7) / E039A(5) / Q026；JP033 这类横排拼版网天然是多片
- 合并 47 个盒型，合格率 **57%**，中位误差约 10%。仍不合格的典型：
  - 原样本：C023(63%)、JP012(42%)、E045(33%)、L015(33%)、Q013(20%)、HC104BD(18%)
  - 新样本：HC104CB(86%)、JP002(75%)、C026(71%)、G008(63%)、R015(61%)、HC010A(65%)、B052(57%)、E013A(50%)、M044(48%)、L059(38%)、L018(25%)
  - E045 / L015 属于结构本身（解析式理想值也是 33%），不是规则问题
  - N001 停在 12%，其 38mm 超出是真实卷边；两条围边已在 v39 修好（不再是独立片）
- 面板细分（沿内部压痕切开过长面板）**对 N001 不成立**——它的侧墙内部连切割线都没有。是否适用于其它盒型待验证
- ⚠️ 评估时务必**换样本**：固定样本迭代会高估约 10 个百分点
- ⚠️ 别拿 `grid`（栅格结果）去证伪/证实栅格本身——那是循环论证。要回到原始 `fe` 数据独立验证（扫射线交点、查端点邻接）
- `de.sl` / `de.dl` 与栅格面积的比值在 25%–74% 间无规律，**不能**用作面积校验
- JP012 的缺口经查是**真实空洞**（V 形切口顶点在 y=130，下方 y=150 是切割线），不是算法 bug
- N046 只能解出 1 个面（数据源本身问题）
- 三个已关掉但保留开关的能力：`Preview3D._useValve`（安全阀）、`_useMult2`（vDepth≥3 折 180°）、`_pickRootByFit`（按 L/W/D 反选根节点）

---

## 六、验证方式

因为当前模型无法查看截图，全部用数值手段验证：

```bash
python -m http.server 8099          # 本地服务
agent-browser open http://localhost:8099/_test3d.html
```

`_test3d.html` 提供 `T3.render(id)`，返回面数/铰链数/岛数/多边形面积比；另可用 `readPixels` 做像素统计确认画面非空白、测量模型屏幕占比。

> ⚠️ 改 `preview3d.js` 后必须递增 `index.html` 和 `_test3d.html` 里的 `?v=N`，否则浏览器读缓存。
